apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: tenant-git-repository
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system-tenant
  url: ssh://git@github.com/rkdutta/tenant-repo